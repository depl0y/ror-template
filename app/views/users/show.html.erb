<div class="col-lg-12">
	<h3><%= t("users.profile.title") %></h3>

	<table class="table">

		<tr>
			<td><%= t("users.profile.attributes.name") %></td>
			<td><%= @current_user.name %></td>
		</tr>
		<tr>
			<td><%= t("users.profile.attributes.email") %></td>
			<td><%= @current_user.email %></td>
		</tr>

	</table>

	<%= link_to t("users.profile.edit"), profile_edit_path, class: "btn btn-primary" %>
</div>